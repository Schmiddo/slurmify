# Simply run your scripts on slurm with slurmify
Super basic, WIP. Specify available partitions and machine configs in `partitions.yaml`, then just call it like this:
`slurmify <partition> <your command>`
